/**
 * 第5章 Java中的锁
 * 5.3. 重入锁
 * 1. 重入锁ReentrantLock，顾名思义，就是支持重进入的锁，它表示该锁能够支持一个线程对资源的重复加锁。
 * 2. 公平性获取锁：公平性与否是针对获取锁而言的，如果一个锁是公平的，那么锁的获取顺序就应该符合请求的绝对时间顺序，也就是FIFO。
 *
 * 5.4 读写锁
 * 1. 之前提到锁ReentrantLock基本上都是排它锁，这些锁在同一时刻只允许一个线程进行访问，而读写锁在同一时刻可以允许多个读线程访问，
 * 但是在写线程访问时，所有的读线程和其他写线程均被阻塞。例如：ReentrantReadWriteLock
 * 2. 读写锁通过一个整型变量维护同步状态，读写锁将变量切分成了两个部分，高16位表示读，低16位表示写，通过位运算改变状态值。
 * 3. 锁降级：是指把持住（当前拥有的）写锁，再获取到读锁，随后释放（先前拥有的）写锁的过程。ReentrantReadWriteLock不支持锁升级。
 *
 * @author 郑元浩
 */
package chapter5;